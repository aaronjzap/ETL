The script "main.py" transforms the data into "customers.csv" and loads  into the database "database.db3", in three tables (customers, emails, phones) which have the same format as the original file.
